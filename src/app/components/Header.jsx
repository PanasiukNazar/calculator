import React from 'react';
import Logo from './Logo.jsx';
import Theme from './Theme.jsx';

const Header = () => {
   return (
      <div className='cap'>
         <Logo />
         <Theme />
      </div>
   );
};

export default Header;
